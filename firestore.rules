rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all users during game operations and demo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
